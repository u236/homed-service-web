[log]
enabled=false
file=/var/log/homed.log

[mqtt]
host=localhost
port=1883
username=
password=
prefix=homed

[server]
port=8080
database=/opt/homed-web/database.json
username=homed
password=homed
guest=guest
